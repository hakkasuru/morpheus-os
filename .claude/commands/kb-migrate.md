---
description: Migrate the knowledge base to a newer OKF spec version
---

Run the kb-migrate runbook (`knowledge/runbooks/kb-migrate.md`), targeting the version named in $ARGUMENTS (or the latest upstream version if none given).
