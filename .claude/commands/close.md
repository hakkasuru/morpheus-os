---
description: Close an item whose MR has been merged
---

For the work item in $ARGUMENTS (status `awaiting-merge`), follow
`workflow/phases/08-close.md`: confirm the merge via the host CLI, then
close the item per `workflow/WORKFLOW.md` § Closing on merge.
