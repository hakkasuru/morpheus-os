---
type: Note
title: "Spring PetClinic architecture (stub)"
description: "Placeholder architecture note for the example repo entry — not a real KB doc yet."
status: draft
created: 2026-08-01
updated: 2026-08-01
stale_after: null # YYYY-MM-DD — re-verify after this date
tags: [spring-petclinic, architecture, stub]
repo: spring-petclinic
generated_by: null # agent name when agent-authored
verified: null # YYYY-MM-DD — set when a human reviews an agent-authored doc
---

# Spring PetClinic architecture (stub)

`spring-petclinic` is one of the two example registry entries this
template ships with (`config/repos.yaml`) — it has not been onboarded via
the `add-repo` runbook, so this file is a placeholder, not researched
content. Do not treat anything below as a claim about the actual repo.

A real `architecture.md`, generated by `add-repo` step 5 from read-only
exploration of the clone under `repos/spring-petclinic/`, would cover:
project structure, entry points, key modules, and data flow. Replace this
stub the first time that runbook runs against this repo (or delete the
entry from `config/repos.yaml` if it is never going to be used for real).
