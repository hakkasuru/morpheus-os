---
mode: agent
description: Migrate the knowledge base to a newer OKF spec version
---

Run the kb-migrate runbook (`knowledge/runbooks/kb-migrate.md`), targeting the version named after this command (or the latest upstream version if none given).
